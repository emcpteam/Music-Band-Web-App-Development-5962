import React from 'react';

// User profile component is now removed for band website
// Users don't need personal profiles on a band's promotional site
// Admin access is handled through the navigation

const UserProfile = () => {
  return null;
};

export default UserProfile;